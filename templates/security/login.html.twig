{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<div class="container mt-5">
    <form method="post" class="needs-validation">
        <!-- Message d'erreur -->
        {% if error %}
            <div class="alert alert-danger">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}

        <!-- Message si l'utilisateur est déjà connecté -->
        {% if app.user %}
            <div class="alert alert-info mb-3">
                Vous êtes connecté en tant que {{ app.user.userIdentifier }}.
                <a href="{{ path('app_logout') }}" class="btn btn-link">Déconnexion</a>
            </div>
        {% endif %}

        <!-- Titre -->
        <h1 class="h3 mb-3 font-weight-normal">Bienvenue dans la Nomenklatura! <br> Veuillez vous connecter</h1>

        <!-- Champ Email -->
        <div class="form-group">
            <label for="email">Adresse email</label>
            <input type="email" id="email" name="_username" class="form-control" required>
        </div>

        <!-- Champ Mot de passe -->
        <div class="mb-3">
            <label for="password" class="form-label">Mot de passe</label>
            <input type="password" name="_password" id="password"
                   class="form-control" autocomplete="current-password" required>
        </div>

        <!-- CSRF Token -->
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

        <!-- Case à cocher "Se souvenir de moi" -->
        <div class="form-check mb-3">
            <input type="checkbox" name="_remember_me" id="_remember_me" class="form-check-input">
            <label for="_remember_me" class="form-check-label">Se souvenir de moi</label>
        </div>

        <!-- Bouton de connexion -->
        <button class="btn btn-lg btn-success" type="submit">
            Se connecter
        </button>
    
    </form>
    <br>
   <a href="" class="btn btn-secondary btn-lg text-white ">Mot de passe oublié?</a>
    <br>
    <br>
    <a href="{{ path('app_register') }}" class="btn btn-lg btn-primary ">S'inscrire</a>
</div>
{% endblock %}
