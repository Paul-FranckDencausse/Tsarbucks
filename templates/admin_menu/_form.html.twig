{{ form_start(form, {'attr': {'class': 'needs-validation'}}) }}
    <div class="mb-3">
        {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
    </div>
    <button class="btn btn-primary btn-lg text-white">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
