{% extends 'base.html.twig' %}

{% block title %}Modifier le Menu{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier le Menu</h1>

    <div class="card shadow-sm p-4">
        {{ include('admin_menu/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ path('app_admin_menu_index') }}" class="btn btn-info btn-lg text-white">Retour à la liste</a>
        {{ include('admin_menu/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
