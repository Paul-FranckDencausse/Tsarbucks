{% extends 'base.html.twig' %}

{% block title %}Modifier les Réservations{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier les Réservations</h1>
<br>
    <div class="card p-4 shadow-sm mb-4">
        {{ include('admin_reservations/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_admin_reservations_index') }}" class="btn btn-info btn-lg text-white">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>

        {{ include('admin_reservations/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
